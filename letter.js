// constructor that takes the selected word and the letters user guesses
// loop through selected word length
// function for if userguessed letter === word[i] then push into array of correct letters
// return correct letters into the new object

module.exports = Letter;